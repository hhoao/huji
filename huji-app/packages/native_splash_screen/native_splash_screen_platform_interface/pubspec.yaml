name: native_splash_screen_platform_interface
description: "A common platform interface for the native_splash_screen plugin."
version: 3.0.0
repository: https://github.com/anicine/native_splash_screen

environment:
  sdk: ^3.4.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_lints:
