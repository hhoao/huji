platform :osx, '14.0'

# CocoaPods analytics sends network stats synchronously affecting flutter build latency.
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

project 'Runner', {
  'Debug' => :debug,
  'Profile' => :release,
  'Release' => :release,
}

def flutter_root
  generated_xcode_build_settings_path = File.expand_path(File.join('..', 'Flutter', 'ephemeral', 'Flutter-Generated.xcconfig'), __FILE__)
  unless File.exist?(generated_xcode_build_settings_path)
    raise "#{generated_xcode_build_settings_path} must exist. If you're running pod install manually, make sure \"flutter pub get\" is executed first"
  end

  File.foreach(generated_xcode_build_settings_path) do |line|
    matches = line.match(/FLUTTER_ROOT\=(.*)/)
    return matches[1].strip if matches
  end
  raise "FLUTTER_ROOT not found in #{generated_xcode_build_settings_path}. Try deleting Flutter-Generated.xcconfig, then run \"flutter pub get\""
end

require File.expand_path(File.join('packages', 'flutter_tools', 'bin', 'podhelper'), flutter_root)

flutter_macos_podfile_setup

target 'Runner' do
  use_frameworks! :linkage => :static

  flutter_install_all_macos_pods File.dirname(File.realpath(__FILE__))
  target 'RunnerTests' do
    inherit! :search_paths
  end
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_macos_build_settings(target)
  end

  # ffmpeg_kit_flutter_new vendors prebuilt ffmpeg frameworks (the
  # ffmpeg-kit-macos-full-gpl-8.0.0 zip its prepare_command downloads). Their
  # arm64 slices were linked on a machine with homebrew zlib and load
  # /opt/homebrew/opt/zlib/lib/libz.1.dylib — a path that only exists where
  # `brew install zlib` ran. CI runners and end-user Macs don't have it, so
  # dyld fails to load the app binary (and any FFI dlopen of it) at runtime.
  # Rewrite the reference to the system libz every macOS install ships.
  pub_cache = ENV['PUB_CACHE'] || File.expand_path('~/.pub-cache')
  ffmpeg_kit_framework_dirs = Dir.glob(
    File.join(pub_cache, 'hosted', '*', 'ffmpeg_kit_flutter_new-*', 'macos', 'Frameworks')
  )
  ffmpeg_kit_framework_dirs.each do |dir|
    Dir.glob(File.join(dir, '*.framework', 'Versions', 'A', '*')).each do |binary|
      next unless File.file?(binary)
      ok = system('install_name_tool', '-change',
                  '/opt/homebrew/opt/zlib/lib/libz.1.dylib',
                  '/usr/lib/libz.1.dylib', binary)
      UI.warn "huji: libz rewrite failed for #{binary}" unless ok
    end
  end
end
